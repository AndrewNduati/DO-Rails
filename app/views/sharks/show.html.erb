<p style="color: green"><%= notice %></p>

<%= render @shark %>

<div>
  <%= link_to "Edit this shark", edit_shark_path(@shark), :class => "btn btn-primary btn-sm" %> <%= link_to "Back to sharks", sharks_path, :class => "btn btn-primary btn-sm" %>

  <div data-controller="posts">
    <button data-action="posts#showAll"> Show Older Posts </button>
    <div data-posts-target="show" style="visibility: hidden">
      <%= render 'sharks/all'%>
    </div>
  </div>

  <%= button_to "Destroy this shark", @shark, method: :delete %>
</div>
